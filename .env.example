# Local runtime configuration is package-owned.
#
# API reference: apps/api/.env.example
# Web reference: apps/web/.env.example
#
# Run `pnpm bootstrap:local-env` for first-time local setup. The generated
# package-local .env.local files remain ignored and must never be committed.
